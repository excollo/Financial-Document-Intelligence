import React from "react";
import { StartConversation } from "@/components/sharedcomponents/StartConversation";

const StartConversationPage: React.FC = () => {
  return <StartConversation />;
};

export default StartConversationPage;
