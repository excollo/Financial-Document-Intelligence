name: Trigger auto deployment for aca-ai-api-prod

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/aca-ai-api-prod-AutoDeployTrigger-00ebf891-c6c2-47e9-9ec0-c584b6d6a529.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ACAAIAPIPROD_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.ACAAIAPIPROD_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.ACAAIAPIPROD_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: drhpacr.azurecr.io
          registryUsername: ${{ secrets.ACAAIAPIPROD_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.ACAAIAPIPROD_REGISTRY_PASSWORD }}
          containerAppName: aca-ai-api-prod
          resourceGroup: DRHP-summarizer
          imageToBuild: drhpacr.azurecr.io/aca-ai-api-prod:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


